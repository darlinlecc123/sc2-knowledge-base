# 贡献指南

感谢你参与维护 **PySC2 Raw API 变量知识库**。

本项目最初从 STORM 项目中提取变量，但公开维护后的目标不再局限于 STORM：任何研究者或开发者，即使从未使用过 STORM，只要能够为 PySC2 Raw API 变量、其直接派生量或项目适配关系提供可核查证据，都可以参与贡献。

贡献的首要目标不是快速增加条目数量，而是让 LLM Agent 能够根据自然语言稳定找到正确变量，并回答：

- 变量叫什么、表示什么；
- 它位于哪一层接口；
- 数据类型、形状、单位、范围或枚举是什么；
- 它来自哪个 API、协议字段、计算公式或项目映射；
- 该结论适用于哪个 PySC2、SC2 Protocol、游戏或项目版本；
- 有哪些源码、运行结果或官方资料可以作为依据；
- 哪些内容是通用事实，哪些只是某个项目的实现约定。

---

## 1. 项目定位

本仓库是一个面向 LLM Agent、代码开发和研究写作的 **PySC2 Raw API 变量与数据流知识库**，不是 STORM 项目的附属说明书，也不是 StarCraft II 百科。

STORM 在仓库中的定位是：

1. 第一批变量记录的来源；
2. Raw Observation 到图结构、动作和实验指标的数据流案例；
3. 一个可验证的项目适配器；
4. 不是后续贡献的准入条件，也不是通用语义的唯一依据。

贡献者不需要安装、运行或了解 STORM。对于通用 PySC2 Raw API 字段，可以仅依据官方接口、锁定版本的源码和可复现运行结果完成贡献。

---

## 2. 新的范围边界

### 2.1 可以进入正式知识库的内容

以下内容均可提出贡献，不要求已被 STORM 使用。

#### A. SC2 Protocol / Raw Observation 原始字段

包括但不限于：

- `ResponseObservation` 及其 Raw Data 字段；
- `ObservationRaw`、`PlayerRaw`、`RawUnit` 等结构中的字段；
- 单位 tag、类型、阵营、位置、生命、护盾、能量、订单、冷却等 Raw API 信息；
- Raw action 所需的 ability、unit tag、target tag、world position 等参数；
- 可从 game data 或 protocol 枚举中稳定解释的 ID、标志位和状态值。

即使当前 STORM 没有读取该字段，只要来源、版本和语义明确，也可以收录。

#### B. PySC2 对协议字段的封装和暴露形式

例如：

- PySC2 observation 对象中的访问路径；
- `named_array`、protobuf、列表、标量或枚举的 Python 表示；
- `action_spec`、`observation_spec` 和 Raw Function 的参数形式；
- PySC2 对底层 SC2 Protocol 字段进行的重命名、转换或兼容处理。

#### C. Raw API 的直接派生变量

允许收录对 Raw API 变量进行确定性计算后得到、且对研究或智能体开发具有复用价值的变量，例如：

- `health_ratio = health / health_max`；
- 单位间距离、相对位置和质心；
- 按 alliance 或 unit_type 过滤后的数量；
- 由单位订单、武器冷却或资源字段产生的状态标志；
- 从 Raw action 参数到 `FunctionCall` 的映射结果。

派生变量必须给出完整输入、公式、边界条件、空值处理和适用范围。项目自定义启发式不得包装成 PySC2 官方定义。

#### D. 项目适配层变量

允许记录 STORM、其他 PySC2 智能体、研究代码或环境封装如何读取、重命名、裁剪、归一化、聚合或消费 Raw API 变量。

此类条目必须带项目命名空间或明确的适用范围，例如：

- `storm.abox.unit.hp`；
- `project_x.observation.enemy_units`；
- 某个环境封装中的 `state_vector[12]` 与 `RawUnit.health` 的映射。

项目适配记录只能证明“该项目如何实现”，不能单独证明“PySC2 通用语义是什么”。

#### E. 与 Raw API 数据流直接关联的实验指标

当指标能够明确回溯到 Raw API 字段或上述派生量时，可以收录，例如胜负、累计奖励、单位损失、动作有效率、预测误差等。

必须说明：

- 指标是环境直接返回、脚本聚合，还是模型预测；
- 计算窗口和聚合方式；
- 是否属于特定项目或实验协议；
- 历史 CSV 只能证明对应实验产物，不能自动代表当前代码。

#### F. 版本差异、缺陷和运行时限制

欢迎贡献：

- 不同 PySC2 / SC2 Protocol / 游戏版本间的字段变化；
- 字段存在但始终为空、按条件出现或语义容易误解的情况；
- 官方定义、PySC2 封装和实际运行表现之间的差异；
- 已复现的接口错误、兼容问题和平台差异。

此类贡献必须标注版本和验证环境，不得把单次观察无条件推广到全部版本。

### 2.2 默认不进入核心变量集的内容

以下内容通常不属于本知识库核心范围：

- 与 Raw API 无直接关系的 Feature Layer、RGB 像素和渲染变量；
- 单纯的兵种百科、剧情、战术攻略或对局常识；
- 游戏引擎隐藏但 observation 中不可访问的“真实值”；
- 神经网络权重、梯度、优化器状态和 replay buffer 内部张量；
- 与 Raw API 数据流无关的 UI、绘图样式或日志格式参数；
- 没有接口、源码、公式或运行依据的记忆性描述；
- 只由 LLM 猜测、无法追溯来源的变量解释；
- 将某个项目的字段名错误描述为 PySC2 官方字段。

如果希望扩展到 Feature Layer、SMAC、SC2LE 其他接口或更完整的游戏知识，应先提交范围提案，设计独立命名空间、Schema 和检索边界，不要在单个变量 PR 中静默扩大范围。

---

## 3. 四层知识模型

为避免“官方字段”和“项目自定义变量”混在一起，新增记录应明确属于哪一层。

| 层级 | 含义 | 示例 | 主要证据 |
|---|---|---|---|
| L0 协议层 | SC2 Protocol 定义的字段、消息和枚举 | Raw unit health、unit tag | 官方 `.proto`、协议版本 |
| L1 PySC2 层 | PySC2 暴露、封装或转换后的接口 | observation 访问路径、Raw Function 参数 | PySC2 源码、锁定版本、运行输出 |
| L2 通用派生层 | 可由 Raw API 确定计算、具有跨项目复用价值的量 | health ratio、单位距离 | 明确公式、测试和输入字段 |
| L3 项目适配层 | STORM 或其他项目的重命名、聚合和业务约定 | ABox hp、动作调度字段 | 对应项目源码、配置和测试 |

实验指标、模型预测和本体变量可以建立在这些层之上，但必须保留到上游 Raw API 字段的依赖关系，并明确它们不是环境原始观测。

同一个概念在不同层级可以有多个记录。例如 `RawUnit.health`、某项目的 `unit.hp` 和 `health_ratio` 不应合并成一个条目，而应通过关系图连接。

---

## 4. 通用事实与项目事实必须分开

每个声明都应回答“这个结论对谁成立”。建议使用以下作用域：

| 作用域 | 说明 |
|---|---|
| `protocol_general` | 由指定 SC2 Protocol 版本定义 |
| `pysc2_versioned` | 只对指定 PySC2 版本或版本范围成立 |
| `runtime_observed` | 在明确环境和版本中实际观察到 |
| `project_specific` | 只对 STORM 或另一个具体项目成立 |
| `needs_verification` | 有候选依据，但尚不足以形成正式事实 |

如果当前 Schema 暂未提供独立的 `scope` 字段，应把作用域写入 `definition`、`limitations`、`evidence.claim` 和验证说明中，并在 Schema 通用化后迁移。不要省略版本边界。

错误示例：

> `armor_ratio` 表示 PySC2 提供的单位护甲比例。

如果它实际只是某项目把 `shield_ratio` 映射成 `armor_ratio`，这就是项目实现事实，而不是 PySC2 通用事实。

正确写法：

> 在项目 X 的版本 Y 中，`armor_ratio` 由 `RawUnit.shield / RawUnit.shield_max` 计算。PySC2 RawUnit 并未据此定义通用的 `armor_ratio` 字段。

---

## 5. 证据要求

### 5.1 证据优先级

对于通用 Raw API 语义，推荐按以下顺序取证：

1. 锁定版本的 SC2 Protocol protobuf 定义或官方协议源码；
2. 锁定版本的 PySC2 源码、生成接口或官方仓库内容；
3. 可复现的 PySC2 / SC2 运行输出与最小验证脚本；
4. 当前项目源码、测试、配置和数据生成脚本；
5. 对应版本的官方文档；
6. 论文、社区说明或百科资料，仅作补充。

对于项目适配层事实，该项目当前可执行源码和测试属于主要证据；但它们不能替代官方来源来证明协议通用语义。

### 5.2 每条正式变量的最低要求

每个新增或实质修改的变量至少应包含：

| 项目 | 最低要求 |
|---|---|
| 稳定 ID | 遵守 taxonomy 与命名规则，不因中文措辞变化重命名 |
| 层级与类别 | 明确是协议、PySC2、通用派生还是项目适配变量 |
| 中英文名称 | 至少包含规范中文名、英文名和代码名 |
| 定义 | 区分游戏含义、接口编码与项目内部语义 |
| 接口 | 数据类型、Python 类型、形状、单位、范围、枚举和空值状态 |
| 版本 | PySC2、SC2 Protocol、游戏或项目版本；未知时明确标记 |
| 来源 | 原始字段、上游变量、静态数据或计算输入 |
| 实现位置 | 文件、消息、类、函数、字段或稳定 symbol |
| 公式 | 派生变量必须列出公式、边界条件和异常处理 |
| 生命周期 | 每 observation、episode、action、静态或实验聚合 |
| 事实状态 | 区分实现事实、运行观察、设计意图和待验证内容 |
| 检索入口 | 中文、英文、代码名、缩写、口语别名及必要负向词 |
| 关系 | 连接上游、下游、映射、归一化、验证或动作执行节点 |
| 限制 | 条件出现、版本差异、歧义和不能推出的结论 |
| 测试 | Schema、关系端点、检索、证据绑定及必要的公式测试 |

不要伪造精确行号。如果上游源码变化频繁，优先记录仓库、版本、相对路径、protobuf message、类、函数或字段 symbol。

### 5.3 不接受的证据方式

- “大模型说这个字段是……”；
- 没有版本号的博客摘录；
- 搜索结果摘要而未核查原始页面；
- 只凭变量名猜测物理含义；
- 用一个第三方项目的实现证明全部 PySC2 版本均如此；
- 只粘贴截图而不提供可检索文本、路径或复现方式；
- 把 README 中的设计说明直接提升为运行时事实。

---

## 6. 当前 Schema 的兼容性说明

当前 `schemas/variable.schema.json` 来源于 STORM V0.1，仍包含以下历史命名：

- `interface.storm_paths`；
- `consumers` 中的 STORM 案例；
- 若干 STORM 派生层类别和验证描述。

这些字段是 **历史兼容字段，不是新范围边界**。

对于不依赖 STORM 的新变量：

1. 不得编造不存在的 STORM 路径以通过 Schema；
2. 不得把 PySC2 路径塞入 `storm_paths` 并假装二者等价；
3. 如果现有 Schema 无法无损表达，应提交配套 Schema 通用化修改；
4. Schema 修改需提供旧记录迁移脚本、兼容策略和完整离线测试；
5. 在新 Schema 发布前，可先通过 Issue 或草案记录提交来源和拟议结构，由维护者决定是否合并。

推荐的未来方向是将项目绑定统一为可重复的 `bindings` 或 `implementations`，每项显式记录 `project`、`version`、`path`、`symbol` 和映射类型，同时保留旧字段的迁移读取能力。

---

## 7. 贡献类型

建议在 Issue 或 PR 标题中标明类型：

- `variable`：新增 Raw API 或派生变量；
- `correction`：修正定义、类型、范围、版本或证据；
- `alias`：补充自然语言、缩写或代码别名；
- `relation`：补充来源、映射、公式或下游关系；
- `adapter`：增加某个开源项目的接口映射；
- `runtime`：提交可复现的运行时观察；
- `schema`：扩展或通用化机器可读 Schema；
- `retrieval`：改进检索、RAG 或上下文生成；
- `benchmark`：新增不泄漏答案的评测问题；
- `docs`：更新说明、示例和术语表；
- `issue`：记录已知缺陷、歧义或版本差异。

仅修改错别字时不要求建立完整变量 Issue；涉及变量语义、范围或接口契约时应先说明证据和影响。

---

## 8. 推荐贡献流程

### 第一步：检索现有知识

先搜索：

- 稳定变量 ID；
- 中文、英文和代码别名；
- 相同 protocol 字段或 PySC2 路径；
- `known_issues.yaml` 中的已知问题；
- 关系图中的上游和下游节点。

不要因为名称不同就建立重复变量。确属不同层级的记录，应创建独立 ID 并建立映射关系。

### 第二步：建立 Issue 或设计说明

说明：

- 要新增或修正什么；
- 它属于四层模型中的哪一层；
- 是否为通用事实或项目事实；
- 适用版本；
- 主要证据；
- 是否影响 Schema、别名、关系、检索和评测。

范围较大的贡献，例如完整补充 RawUnit 未覆盖字段、增加新项目适配器或调整 Schema，应先讨论设计再批量提交。

### 第三步：先修改机器可读数据

机器可读记录是事实源。根据影响同步检查：

- `variables/*.yaml`；
- `schemas/variable.schema.json`；
- `lineage/variable_lineage.yaml`；
- `relations/variable_relations.jsonl`；
- `retrieval/aliases.yaml` 及查询模式；
- `known_issues.yaml`；
- 相关测试和 benchmark。

自动生成的目录、Markdown 和 LLM context 应由生成脚本更新，不要手工制造与变量记录不一致的副本。

### 第四步：补充检索表达

至少考虑：

- 中文规范名和口语表达；
- 英文名、代码字段和完整访问路径；
- 常见缩写、拼写变体和历史名称；
- 容易混淆的同名字段；
- 应拒绝或降低排序的范围外表达。

别名不能改变变量事实。例如“护甲”可能是自然语言误称，应通过歧义说明处理，而不是直接把错误语义写入定义。

### 第五步：添加测试

至少验证：

- YAML/JSON 可解析并符合 Schema；
- 所有关联变量 ID 存在；
- 证据 ID 与声明绑定正确；
- 典型自然语言问题可以召回目标变量；
- 歧义问题不会错误命中；
- 派生公式的输入、零值和缺失值行为正确；
- 项目适配事实不会被回答成 PySC2 通用事实。

### 第六步：提交 PR

PR 中应列出：

- 修改摘要；
- 收录层级和范围；
- 适用版本；
- 新增或修改的变量 ID；
- 证据来源；
- 实际运行的验证命令及真实输出；
- 对检索、Schema、上下文包和兼容性的影响；
- 未验证项目与已知限制。

不要声称未实际运行的命令已经通过。

---

## 9. 本地验证

以下命令的目的，是先检查 Python 文件语法和 GitHub 发布边界。请从知识库根目录运行：

```powershell
python -m py_compile scripts/validate_github_release.py
python scripts/validate_github_release.py
```

如需验证离线检索，可临时设置本地包路径后查询一个已知变量：

```powershell
$env:PYTHONPATH = "src"
$env:PYTHONIOENCODING = "utf-8"
python -m sc2kb.cli --limit 3 --json "单位当前血量对应哪个字段"
```

如果修改了变量记录、关系、检索规则、Schema、上下文生成器或发布包，还应运行对应生成与验证脚本。当前仓库的测试入口可能随版本演进，PR 应记录实际使用的脚本名称和结果，而不是假定存在某个未配置的测试框架。

涉及 SC2 运行时验证时，还应记录：

- 操作系统；
- Python 版本；
- PySC2 版本；
- SC2 Protocol 或游戏版本；
- 地图与接口配置；
- `use_raw_units`、`use_raw_actions` 等关键选项；
- 最小复现脚本；
- 实际观察结果和未覆盖条件。

运行一次得到的结果应标为 `runtime_observation`，除非有更高等级来源证明它是稳定接口契约。

---

## 10. PR 审查门禁

维护者至少检查以下项目。

### 范围与分层

- 是否与 PySC2 Raw API 或其可追溯下游直接相关；
- 是否正确区分协议层、PySC2 层、通用派生层和项目适配层；
- 是否错误地把项目变量写成通用字段；
- 非 STORM 贡献是否被公平审查，而不是以“STORM 未使用”为由拒绝。

### 事实与证据

- 是否存在适用于该声明的原始来源；
- 是否记录版本、平台和运行条件；
- 是否区分 `design_intent`、`code_reality`、`runtime_observation`、`derived_runtime` 和 `needs_verification`；
- 公式、范围、枚举、空值和条件出现是否有依据；
- 冲突来源是否被显式保留和解释。

### 数据一致性

- 稳定 ID、类别、别名和关系端点是否一致；
- 上下游关系是否闭合；
- 证据是否真正支持所绑定的声明；
- 生成文档和机器记录是否一致；
- Schema 变化是否提供迁移与兼容测试。

### 安全与评测

- 是否包含密钥、`.env`、私人绝对路径、日志、缓存或模型私有配置；
- 是否提交大体积二进制文件或无法复现的运行产物；
- 是否根据冻结盲测答案进行针对性调参；
- 是否降低评测门槛、覆盖旧收据或删除失败结果；
- 是否清楚说明模型 API、费用和外部依赖。

---

## 11. 冲突事实的处理

协议定义、PySC2 封装、项目代码和运行结果可能不一致。遇到冲突时不要强行只保留一个答案，应：

1. 分别记录每条声明及其作用域；
2. 为每条声明绑定独立证据；
3. 标明版本和验证日期；
4. 说明哪个是协议定义、哪个是当前实现、哪个是运行观察；
5. 在 `known_issues.yaml` 中建立冲突或兼容性问题；
6. 让检索结果能够向用户解释差异，而不是悄悄覆盖旧事实。

通常，官方协议定义用于解释字段设计；锁定版本的 PySC2 源码用于解释 Python 接口；当前项目源码用于解释项目行为；运行结果用于证明特定配置下的实际表现。它们解决的问题不同，不应互相冒充。

---

## 12. 版本策略

采用语义化版本思想：

- **PATCH**：定义澄清、别名补充、证据路径修正、已知问题补充、非破坏性检索修复；
- **MINOR**：增加新的 Raw API 字段族、通用派生变量、项目适配器、关系类型或向后兼容的 Schema 能力；
- **MAJOR**：稳定 ID、Schema、接口契约、命名空间或知识范围发生破坏性变化。

现有 V0.1 的“STORM 当前使用变量”是一个已发布数据快照，不再是未来贡献的永久上限。扩展范围时应保留版本说明，使用户能够区分：

- V0.1 已验证的 STORM 子集；
- 后续新增的通用 PySC2 Raw API 记录；
- 不同项目提供的适配记录；
- 仅在特定版本或环境中验证的运行事实。

冻结 challenge 的题目、答案或评分协议发生变化时，不得静默覆盖。必须保留旧版本、哈希、首次测试收据和变更原因。

---

## 13. 提交内容与隐私

请勿提交：

- API Key、访问令牌、密码或 `.env`；
- 私有模型地址和带凭据的配置；
- 包含用户名的本机绝对路径；
- 未清理的模型完整响应、日志和缓存；
- 未获许可的游戏资源、地图或受限数据；
- 无必要的大型二进制文件。

提交运行证据前，应对账号、路径、主机名和密钥做脱敏。引用外部项目源码时遵守其许可证，只记录必要的接口事实和短小证据，不复制不必要的大段受版权保护内容。

---

## 14. 行为规范

请使用专业、可复核和尊重性的语言。技术争议应回到版本、源码、协议、测试和复现实验，不以模型猜测、个人资历或项目归属替代证据。

本项目欢迎 STORM 使用者，也同样欢迎从未接触过 STORM 的 PySC2、SC2LE、强化学习、智能体和知识工程研究者。只要贡献能够明确回答“变量是什么、从哪里来、适用于什么范围、证据是什么”，就应得到一致、公平的审查。
