# 路线图

## 原则

路线图按“证据可靠性优先于覆盖数量”推进。日期不是承诺；每个里程碑只有在验收条件满足后才能标记完成。

## V0.1 候选版：知识库骨架与内部验收

状态：**已完成主体工作，公开发布 no-go**。

已完成：

- 135 个正式变量和 1430 条关系；
- 别名、关系、已知问题和证据血缘；
- 本地检索、LLM 上下文、QA Agent 和离线测试；
- 多模型三条件评测；
- minimal/full 确定性候选包；
- 内部验收与公开发布材料。

公开前阻塞项：

1. 确认版权主体、作者列表和正式许可证；
2. 修复 full 包在干净目录中的测试导入路径；
3. 修复 Windows 默认编码下的 CLI JSON 输出；
4. 更新或归档状态过时的根 README；
5. 在 Python 3.12 + uv 环境执行发布验收。

## V0.1.1：可移植性和检索修复

计划：

- 修复 `environment.step_mul` 自然语言改写漏召回；
- 让 full 包成为可安装、可在干净目录直接测试的 Python 项目；
- 增加 Windows/Linux 跨平台 UTF-8 测试；
- 更新过时的步骤 16 planned-state 验证器；
- 建立只允许知识库候选文件进入 Git 的 CI 门禁。

## V0.2：重新验证有效 RAG

计划：

- 仅在非 challenge 开发集改进多变量、公式依赖、歧义和范围判断；
- 冻结全新 challenge，保留题集哈希和首次盲测收据；
- 以变量召回率、变量精确率、证据命中率、拒答准确率和原生 JSON 合规率共同验收；
- 若未通过全部预声明门槛，继续如实报告，不把回归结果冒充泛化结果。

## V0.3：社区维护能力

计划：

- 自动生成变量目录和数据流文档；
- 建立变量变更影响分析；
- 增加 PySC2/SC2 版本兼容矩阵；
- 形成可公开复用的基准协议，但不公开会破坏盲测有效性的隐藏答案；
- 建立维护者轮值和版本弃用策略。

## 长期方向

在获得明确范围授权和充分证据后，再讨论扩展到其他 STORM 场景或 PySC2 接口层。不会默认把 V0.1 扩展成完整 StarCraft II 百科。
